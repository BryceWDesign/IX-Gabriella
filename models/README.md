# Models Directory

This directory intentionally does not contain large model weights.

Use it for model manifests, small sample configs, and notes only. Real model weights should be stored outside the main IX-Gabriella repo.
